apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "providers-functions.fullname" . }}-test-connection"
  labels:
    {{- include "providers-functions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "providers-functions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
