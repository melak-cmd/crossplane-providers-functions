apiVersion: v1
kind: Service
metadata:
  name: {{ include "providers-functions.fullname" . }}
  labels:
    {{- include "providers-functions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "providers-functions.selectorLabels" . | nindent 4 }}
